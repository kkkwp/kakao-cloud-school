-- SET

-- DEPTNO와 EMP 테이블의 DEPTNO의 합집합
SELECT DEPTNO
FROM DEPT
UNION
SELECT DEPTNO
FROM EMP;

SELECT DEPTNO
FROM DEPT
UNION ALL
SELECT DEPTNO
FROM EMP;

-- DEPTNO와 EMP 테이블의 DEPTNO의 교집합
SELECT DEPTNO
FROM DEPT
INTERSECT
SELECT DEPTNO
FROM EMP;

-- DEPTNO와 EMP 테이블의 DEPTNO의 차집합
SELECT DEPTNO
FROM DEPT
EXCEPT
SELECT DEPTNO
FROM EMP;